"use strict";
var BallTranslateSimulation;
(function (BallTranslateSimulation) {
    function solution(n, m, x, y, queries) {
        const curPos = [x, y];
        // while(true){
        //     const [direction, offset] = queries.pop() as [number, number];
        //     if(direction >=2){//행 연산
        //         if()
        //     }else{//열 연산
        //     }
        // }
    }
})(BallTranslateSimulation || (BallTranslateSimulation = {}));
