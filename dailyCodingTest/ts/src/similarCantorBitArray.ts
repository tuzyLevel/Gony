namespace SimilarCantorBitArray {
  function solution(n: number, l: number, r: number) {
    var answer = 0;
    const lGroup = Math.floor(l/5);
    const rGroup = Math.floor(r/5);
    

    


    return answer;
  }
}
